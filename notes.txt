!!! - автоматически генерировать виджет ноды на основе абстрактного класса, отказ от написания виджетов нод индивидуально
!!! - property view для ноды. По выделению обновлять вид. Вид - член класса виджет, и он один единственый


!!! - Написать метод виджета CreateNode. Потом использовать для фомата хранения графов +++

!!! - Напистаь метод connect attr

1) операции со стрингами(append+, replace+, upper+, lower+, capitalize+, string contains+)
2) ноды конверторы(num to string+, string to int+, string to float+)
3) массивы, операции с массивами(MakeArray+, ArrayAppend+, ArrayRemoveElement, IsInArray, ArrayGetElement, IsArray, ArrayInsert, ClearArray)

встроенные функции питона (max, min, any, all)
нода перемешивающая массив (random.shuffle)

5) прокачать nodebox, категории
9) привязка к сетке
4) 3d вьюпорт для визуализации
6) написать нод визард
7) скриншот сцены



Console commands:
                createNode className x y name +++
                killNode nodeName +++
                move nodeName x y +++
                select nodeName +++
                connectAttr srd dst +++
                disconnectAttr AttrName +++
                setAttr +++
                commentSelected +++
    createComment
    copy
    paste
